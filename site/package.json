{
  "name": "txx-actions-site",
  "private": true,
  "type": "module",
  "scripts": {
    "data": "bun scripts/build-actions.ts",
    "dev": "bun run data && vite",
    "build": "bun run data && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "mithril": "^2.2.8"
  },
  "devDependencies": {
    "@types/mithril": "^2.2.7",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
